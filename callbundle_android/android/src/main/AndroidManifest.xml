<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.callbundle.callbundle_android">

    <!-- Permissions automatically merged into app manifest -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application>
        <!-- Native incoming call Activity shown over the lock screen.
             Only THIS Activity has lock screen flags — the main Flutter
             Activity does NOT, preventing full app access over lock screen. -->
        <activity
            android:name=".IncomingCallActivity"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- ConnectionService for TelecomManager integration -->
        <service
            android:name=".CallConnectionService"
            android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.ConnectionService" />
            </intent-filter>
        </service>

        <!-- BroadcastReceiver for notification Accept/Decline/End actions -->
        <receiver
            android:name=".CallActionReceiver"
            android:exported="false" />
    </application>
</manifest>
